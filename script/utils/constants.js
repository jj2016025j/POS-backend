const METRICS = Object.freeze({
    SALES_QUANTITY: 'salesQuantity',
    SALES: 'sales'
});

const TIME_UNITS = Object.freeze({
    DAY: 'day',
    MONTH: 'month',
    YEAR: 'year'
});

module.exports = { METRICS, TIME_UNITS };
